import type { Metadata, Viewport } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'

import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-playfair',
})

export const metadata: Metadata = {
  title: 'Phoenix Family Law | Cleveland Divorce & Family Law Attorneys',
  description:
    'Experienced Cleveland family law attorneys providing strategic legal guidance in divorce, child custody, support, and property division throughout Northeast Ohio.',
}

export const viewport: Viewport = {
  themeColor: '#1a1e28',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
